import React from 'react';
import Button from 'react-bootstrap/Button';
const Bootstrap = () => {
    return (
        <div>
            <Button variant="secondary">Boostrap Button</Button>
        </div>
    );
};

export default Bootstrap;